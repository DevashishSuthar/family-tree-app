import defaultAvatar from '../assets/images/default-avatar.png';

// Imported (not a public/ path) so Vite resolves and hashes it correctly in the build.
export const IMG_DEFAULT_AVATAR = defaultAvatar;